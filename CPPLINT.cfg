# cpplint configuration for this repository
# Stop searching for parent configs outside this repo
set noparent

# Disable the rule: "Include the directory when naming header files"
# This suppresses errors like: [build/include_subdir] [4]
filter=-build/include_subdir
